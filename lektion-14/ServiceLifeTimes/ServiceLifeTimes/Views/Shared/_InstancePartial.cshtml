@model InstanceService
@{
    var service = new InstanceService();
}

<h5 class="mt-4">Partial View</h5>
<div class="mb-3">
    <div class="text-muted"><small>Modell</small></div>
    <div style="font-size: 1.5em">@Model.GetId()</div>
</div>
<div class="mb-3">
    <div class="text-muted"><small>Instanse new Service()</small></div>
    <div style="font-size: 1.5em">@service.GetId()</div>
</div>